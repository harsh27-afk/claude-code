## Basics :

- scenario : already half baked project is given to u and u continue from that point 

- bash mode : shift + ! 

- use the bash mode as claude will also know what u did, instead of executing commands in seperate window !!

- claude code (is the coding agent) it can be connected to some other models as well !! even opensource local running models as well, the condition is that claude code (agentic ai) should be installed on ur machine.
ollama opensource models

- they (ollama) provide local and cloud models, cloud ones (are paid as they are maintaining servers)

- **To start a new session use the command : claude   / prev session :** 

